{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.morph.wither.look_at_target": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": [ "wither_look_at_target_default" ],
          "transitions": [
            { "gliding": "query.is_gliding" },
            { "swimming": "query.is_swimming || query.is_crawling" }
          ]
        },
        "gliding": {
          "animations": [ "wither_look_at_target_gliding" ],
          "transitions": [
            { "swimming": "query.is_swimming || query.is_crawling" },
            { "default": "!query.is_gliding" }
          ]
        },
        "swimming": {
          "animations": [ "wither_look_at_target_swimming" ],
          "transitions": [
            { "gliding": "query.is_gliding" },
            { "default": "!query.is_swimming && !query.is_crawling" }
          ]
        }
      }
    }
  }
}
